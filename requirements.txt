# Semantic Image Compression Framework — not_text_processor branch
# Validated working environment: Python 3.12, CUDA 12.1, RTX 3050 6GB
#
# Install order matters — see README.md for full setup instructions.
# torch/torchvision must be installed FIRST from the CUDA index before
# other packages, otherwise pip may pull a CPU-only build.
#
#   pip install torch==2.5.1 torchvision==0.20.1 --index-url https://download.pytorch.org/whl/cu121
#   pip install -r requirements.txt
#
# flash_attn requires a matching CUDA toolkit (nvcc) available in the
# environment to install a prebuilt wheel. If missing:
#   conda install -c nvidia cuda-toolkit=12.1

transformers==4.38.0
easyocr==1.7.2
pillow==12.2.0
einops==0.8.2
timm==1.0.27
flash_attn==2.8.3.post1
tiktoken==0.13.0
numpy==2.4.4
opencv-python-headless==4.13.0.92
